/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package Qtrip_API.app;

import Qtrip_API.list.LinkedList;

import static Qtrip_API.utilities.StringUtils.join;
import static Qtrip_API.utilities.StringUtils.split;
import static Qtrip_API.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
